{
  "name": "nvim",
  "version": "0.1.0",
  "description": "nvim config started from lazyvim",
  "main": ".",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install"
  },
  "author": "Alex Russell",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
